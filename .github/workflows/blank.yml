name: Candle!
on:
  schedule:
  - cron: "0/15 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-ruby@v1
    - run: ruby main.rb
